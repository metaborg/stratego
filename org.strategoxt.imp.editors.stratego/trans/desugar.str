module desugar

rules

  basic-desugar:
    SDef(x, a*, s) -> SDefT(x, a*, [], s)

  basic-desugar:
    RDef(x, a*, s) -> SDefT(x, a*, [], s)

  basic-desugar:
    SDefNoArgs(x) -> SDefT(x, [], [], s)

  basic-desugar:
    RDefNoArgs(x, s) -> SDefT(x, [], [], s)

