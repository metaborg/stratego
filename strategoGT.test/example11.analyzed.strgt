specification

strategies

s =
  fail

returns-a-list :: ? -> List(?)

returns-a-list =
  fail

do-something =
  fail

returns-a-pair :: ? -> ? * ?

returns-a-pair =
  fail

something-else :: ? * ? -> ?

something-else =
  fail

map :: (? -> ?) List(?) -> List(?)

map(s) =
  fail

Snd :: ? * ? -> ?

Snd =
  fail

local-variable-type-inference:
  a -> b
    where
      if <s> a then (c := <returns-a-list> a; id); <map(do-something)> c else (c := <returns-a-pair> a; id); <Snd; do-something> c end; b := <cast(Tuple(?, ?)); something-else> c; id