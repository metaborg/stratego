package mb.stratego.compiler.pack.cmd.arguments;

import com.beust.jcommander.Parameters;

@Parameters(separators = "=", commandDescription = "Pack strategy definitions into a CTree for generating boilerplate")
public class Boilerplate extends SharedArguments {
}