module system/io/process

/**
 * Terminating the Current Process
 */
strategies

  /**
   * Abort execution with exit value n.
   *
   * @type Int -> _
   */
  exit :: int -> a
  exit =
    ?n; prim("SSL_exit", n)
