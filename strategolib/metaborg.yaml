---
id: org.metaborg:strategolib:${metaborgVersion}
name: stratego-lib
metaborgVersion: 2.6.0-SNAPSHOT
metaborgBaselineVersion: 2.5.16
dependencies:
  compile:
  - org.metaborg:org.metaborg.meta.lang.esv:${metaborgBaselineVersion}
  # TODO: change to metaborgBaselineVersion in 2.5.17 so this library can eventually be used in the compiler directly (and not the baseline version)
  - org.metaborg:stratego.lang:${metaborgVersion}
pardonedLanguages:
- EditorService
language:
  sdf:
    enabled: no
exports:
- language: StrategoLang
  directory: src-gen/java/strategolib/trans
  includes: "strategolib.str2lib"