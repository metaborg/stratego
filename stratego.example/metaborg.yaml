---
id: org.metaborg:stratego.example:${metaborgVersion}
name: StrategoLang
dependencies:
  compile:
  - org.metaborg:stratego.lang:${metaborgVersion}
build:
  useBuildSystemSpec: true
language:
  stratego:
    args:
    - -la
    - stratego-lib
