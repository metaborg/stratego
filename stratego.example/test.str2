module test

signature
  sorts
    Term
  constructors
    Nil 	: 				         List(a)
    Cons 	: a * List(a) 	-> List(a)
    Car1 	: a 			      -> List(a)
    Car2  : a             -> List(a)
    Car3  : a             -> List(a)

rules
  uncar: Car1(a) -> Cons(a, Nil())
  uncar: Nil() -> Nil()
  uncar = strat
  strat = id
  uncar: Car(a) -> Cons(a, Nil()) // should be skipped (equal)
  uncar: Car(b) -> Cons(b, Nil()) // should be skipped(?) (equal up to var names)
  uncar: Car(c) -> Cons(c, Nil())
  uncar(x): Car(a) -> Cons(a, Nil())
