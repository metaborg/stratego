module multimatch2java

imports
  strj/multimatch/multimatch2matrix
  strj/multimatch/matrix2dfa

  signatures/core/strategies-sig

  libstratego-aterm

strategies
  multimatch2dfa: mm@MultiMatch(_, _) -> dfa
  with
    <debug(!"[i] m2m "); multimatch2matrix; debug(!"[o] m2m ")> mm => matrix
  ; <debug(!"[i] m2d "); matrix2dfa; debug(!"[o] m2d ")> matrix => dfa
  where
    <pp-aterm; debug> dfa
